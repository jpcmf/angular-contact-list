// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBI54M1of_EEfil93r2kOam1fn2kBs-yds",
    authDomain: "contatos-compacto-records.firebaseapp.com",
    databaseURL: "https://contatos-compacto-records.firebaseio.com",
    projectId: "contatos-compacto-records",
    storageBucket: "",
    messagingSenderId: "576748760934",
    appId: "1:576748760934:web:1d8f244cf4d09f76"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
